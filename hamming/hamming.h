#if !defined(HAMMING_H)
#define HAMMING_H

#include <string>
#include <unordered_set>
#include <stdexcept>

namespace hamming {
	int compute(const std::string&, const std::string& );
}  // namespace hamming

#endif // HAMMING_H